<%= form_for(tasktracker) do |f| %>
  <% if tasktracker.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tasktracker.errors.count, "error") %> prohibited this tasktracker from being saved:</h2>

      <ul>
      <% tasktracker.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :approval %>
    <%= f.check_box :approval %>
  </div>

  <div class="field">
    <%= f.label :picture %>
    <%= f.text_field :picture %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description %>
  </div>

  <div class="field">
    <%= f.label :card_id %>
    <%= f.text_field :card_id %>
  </div>

  <div class="field">
    <%= f.label :task_id %>
    <%= f.text_field :task_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
