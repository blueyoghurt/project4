<%= form_for(ngo_user) do |f| %>
  <% if ngo_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ngo_user.errors.count, "error") %> prohibited this ngo_user from being saved:</h2>

      <ul>
      <% ngo_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :ngo_id %>
    <%= f.text_field :ngo_id %>
  </div>

  <div class="field">
    <%= f.label :user_id %>
    <%= f.text_field :user_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
