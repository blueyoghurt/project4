<%= form_for(school_user) do |f| %>
  <% if school_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(school_user.errors.count, "error") %> prohibited this school_user from being saved:</h2>

      <ul>
      <% school_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :school_id %>
    <%= f.text_field :school_id %>
  </div>

  <div class="field">
    <%= f.label :user_id %>
    <%= f.text_field :user_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
